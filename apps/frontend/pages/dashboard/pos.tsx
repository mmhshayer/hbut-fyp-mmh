import Container from '@mui/system/Container';
import { NextPage } from 'next';
import { Pos } from '../../components/pos';

const POS: NextPage = () => {
    return (
        <Container>
            <Pos />
        </Container>
    );
};

export default POS;
