import { Box } from '@mui/material';
import { PageProps } from '../../shared/page.interface';

export default function ProductCard({ sx }: PageProps) {
    return (
        <Box sx={sx}>
            <h1>Add Product</h1>
        </Box>
    )
};